# Local Development Environment Configuration (without Docker)
NODE_ENV=development
PORT=3000

# Redis Configuration (localhost)
REDIS_HOST=localhost
REDIS_PORT=6379

# RabbitMQ Configuration (localhost)
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672

# Execution Configuration
EXEC_TIMEOUT_MS=15000
